// Objeto com dados de uma pessoa
const pessoa = {
  nome: "João",
  idade: 30
};

// Acessar dados
console.log(pessoa.nome); // João

// Array de números
const numeros = [1, 2, 3, 4];

// Acessar o primeiro elemento
console.log(numeros[0]); // 1
