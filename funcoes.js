// Função normal que soma dois números
function soma(a, b) {
  return a + b;
}

// Arrow function que faz o mesmo
const somaArrow = (a, b) => a + b;

console.log(soma(3, 5));      // 8
console.log(somaArrow(3, 5)); // 8
